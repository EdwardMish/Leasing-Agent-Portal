import pagedResponse from './pagedResponse';
import pagedResponseResults from './pagedResponseResults';
import wrapper from './wrapper';

export default {
    pagedResponse,
    pagedResponseResults,
    wrapper,
};
