import configuration from './communicationsConfiguration';
import newsConfiguration from './newsConfiguration';
import Page from './Pages/CommunicationsPage';

export default {
    configuration,
    newsConfiguration,
    Page,
};
