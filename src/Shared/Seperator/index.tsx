import * as React from 'react';

export default () => (
    <div style={{
        backgroundColor: 'var(--color-PECOOrange)',
        height: '.5rem',
        marginTop: '1rem',
        marginBottom: '1rem',
        clear: 'both',
    }}
    />
);
